---
title: 管理画面
description: Windmill の UI を capture-scheduler の実データで歩く —— スクリーンショット付き
---

import { Image } from "astro:assets";
import login from "../../../assets/windmill-ui/01-login.png";
import workspaces from "../../../assets/windmill-ui/02-workspaces.png";
import home from "../../../assets/windmill-ui/03-home.png";
import runsList from "../../../assets/windmill-ui/04-runs-list.png";
import runFlow from "../../../assets/windmill-ui/05-run-flow.png";
import runStep from "../../../assets/windmill-ui/06-run-step.png";
import errProto from "../../../assets/windmill-ui/07-run-error-proto.png";
import errReport from "../../../assets/windmill-ui/08-run-error-report.png";
import flowDetail from "../../../assets/windmill-ui/09-flow-detail.png";
import scriptForm from "../../../assets/windmill-ui/10-script-form.png";
import schedules from "../../../assets/windmill-ui/11-schedules.png";
import variables from "../../../assets/windmill-ui/12-variables.png";
import resources from "../../../assets/windmill-ui/13-resources-states.png";
import trigger from "../../../assets/windmill-ui/14-trigger-script.png";

Windmill は Web の管理画面を同梱している。この配備ではそれがそのまま
capture-scheduler の管理画面になる。別に何かを立てる必要はない。

スクリーンショットは全部この repo の実データ（今日の失敗 → 成功の run 履歴）で、
`scripts/docs-shots.ts` が撮った生成物。手で撮った絵は 1 枚も無い ——
スクリーンショットは UI の書き写しで、書き写しは腐るため。撮り直しの手順は
[開発](/development/) を見ること。

## 開き方

```
http://127.0.0.1:8000
```

|          | 既定                 | 上書き              |
| -------- | -------------------- | ------------------- |
| email    | `admin@windmill.dev` | `WINDMILL_EMAIL`    |
| password | `changeme`           | `WINDMILL_PASSWORD` |

<Image src={login} alt="Windmill のログイン画面。既定の資格情報が入力欄に補完されている" />

port は **loopback に閉じてある**。compose のコメントの通り、ここに届く者は
workspace を作り替えられる —— schedule も webhook も secret も UI から
書き換えられるので、他のマシンには出さない。

ログインすると workspace を選ぶ。bootstrap が作った `crawler` を選ぶ。

<Image src={workspaces} alt="workspace 選択画面。crawler と admins が並ぶ" />

## ホーム —— sidebar が地図

`crawler` に入ると sidebar が出る。このページで歩くのは Home / Runs /
Schedules / Variables / Resources の 5 つ。

<Image
  src={home}
  alt="crawler workspace のホーム。左に Home・Runs・Variables・Resources・Schedules の sidebar"
/>

UI でやってよいのは「見る」「1 回だけ手で起こす」「一時的に止める」まで。
**恒久的な変更は repo を直して push する。** schedule の `enabled` も
`daily.schedule.yaml` に入っているので、UI で止めても次の `windmill:push` で
動き出す。同期が**両方向とも削除**であることは [開発](/development/) を見ること。

## Runs —— まず履歴を読む

**クロールが動いたか / 失敗したかは、ここが最初の一枚。** 各行が 1 実行で、
左端の印が成否、Path が段の種類（`f/waggle/crawl_level` が flow 本体、
`crawl_host` などがその中の段）。`daily` の行は schedule が起こしたもの。

<Image src={runsList} alt="Runs の一覧。crawl_level の成功（緑）と失敗（赤）が時刻順に並ぶ" />

行をクリックすると詳細に入る。**成功した crawl_level** の run を開くと、
上に成否と所要時間、下に flow が受け取った入力（`crawl_id`・`depth`・
辿るべき URL の `frontier`）が並ぶ。

<Image
  src={runFlow}
  alt="成功した crawl_level の run 詳細。Successfully ran の帯と、frontier などの入力"
/>

flow の中の 1 段（ここでは `report_level`）を開くと、その段だけの入力と結果が見える。
段は独立して動くので、**どの段が何を受け取り何を返したか**をここで追える。

<Image src={runStep} alt="report_level の段の run 詳細。crawl_id と results の入力" />

## 失敗を掘る —— 実例 2 つ

失敗は段ごとに違う場所で違う理由で起きる。**Runs で段と error を見て、
中身は各サービスのログへ** というのが掘り方。今日 2 つ踏んだ。

**① `crawl_host` が `Resource not found`。** browserhive の gRPC の
`.proto` を写した resource `u/admin/browserhive_proto` が無いとこうなる ——
`windmill:push` は script を入れるが proto は入れないため。直すのは
`windmill:push-proto`（下の[変数の台帳](#変数の台帳--どの-script-が何を入れるか)）。

<Image
  src={errProto}
  alt="crawl_host の失敗 run。Result に Resource not found at u/admin/browserhive_proto"
/>

**② `report_level` が capture-ledger の `POST /api/crawls/…/pages` に対して
`500` を受け取った。** Runs が言うのはここまで —— **500 の中身は
capture-ledger のログにしか無い**。UI で分かるのは「どの段が・どの宛先に・
何のステータスで落ちたか」で、原因の特定は宛先側のログに渡す、という切り分けになる。

<Image
  src={errReport}
  alt="report_level の失敗 run。Result に POST /api/crawls/…/pages → 500 internal error"
/>

## Flow と script —— 何をするものか

`f/waggle/crawl_level` を開くと、flow の**説明**（capture-ledger が投げた 1 段を
処理して報告する / **1 段で終わる** / 礼儀の 3 点 / per-key concurrency は CE では
効かない）と、受け取る引数が読める。ここに書いてある思想は
[リンクを辿る](/crawl/) と [Windmill CE](/windmill-ce/) の裏付けそのもの。

<Image src={flowDetail} alt="crawl_level flow の詳細。説明文と Crawl Id・Depth・Frontier の入力" />

flow が for-loop で各 host に対して呼ぶのが `crawl_host` の script。
「1 ホストぶんを逐次で取り込む」「単体で叩くものではない」と説明にある通り、
**flow の部品**で、単独で起こすものではない。

<Image src={scriptForm} alt="crawl_host の script 詳細。flow の部品である旨の説明と入力仕様" />

script のコードは UI の Edit でも repo の `windmill/f/waggle/` でも読めるが、
**正は repo**。UI で直すと次の `windmill:push` で戻る。

## Schedules —— いつ走るか

`f/waggle/daily` が唯一の schedule。cron `0 0 4 * * *`（毎日 04:00
Asia/Tokyo）で `trigger_crawl` を起こす。右のトグルが `enabled`。

<Image src={schedules} alt="Schedules。f/waggle/daily の cron・タイムゾーン・有効トグル" />

**トグルで止めても、`daily.schedule.yaml` の `enabled: true` が正なので
次の `windmill:push` で戻る。** 恒久的に止めるなら yaml を直して push する。
意味は [いつ走るか](/schedule/) を見ること。

## Variables と Resources

`u/admin/*` の 4 変数。`waggle_token` は secret なので値がマスクされて出る
（`••••`）—— **マスクされていること自体**が「UI に token を晒していない」の証拠。

<Image
  src={variables}
  alt="Variables。waggle_token は値がマスク、他は browserhive_target などが見える"
/>

`browserhive_proto` は Variables ではなく Resources に居る。ただし
`resource_type: state` なので既定の Workspace タブには出ない ——
**States タブ**に切り替えると見える。失敗①はこれが無い状態だった。

<Image
  src={resources}
  alt="Resources の States タブ。browserhive_proto が resource type state で 1 件"
/>

## 手で 1 回だけ起こす

`trigger_crawl` を開くと Run のフォームがある。schema の説明が日本語で読める
（`waggle_url` = コンテナから見た capture-api、`token` = dev issuer の JWT、
`limit` を省くと enabled な行を全部）。

<Image src={trigger} alt="trigger_crawl の Run フォーム。Waggle Url・Token・Limit の入力欄" />

ただし **schema の `default` は UI からの実行にしか埋まらない**（CE の罠。
[Windmill CE](/windmill-ce/) を見ること）。schedule や webhook からの実行には
届かないので、`daily` は変数から値を渡している。

## 変数の台帳 —— どの script が何を入れるか

`windmill:push` が入れるのは **git にあるものだけ**（script・flow・schedule）。
変数とリソースは別の script が入れる。ここを混同すると、push が緑なのに
flow が `Resource not found` で落ちる（上の失敗①）。

| 置き場                       | 中身                                                         | 入れる script                   |
| ---------------------------- | ------------------------------------------------------------ | ------------------------------- |
| `u/admin/waggle_token`       | capture-ledger への JWT (secret)                             | `windmill:capture-ledger-token` |
| `u/admin/waggle_api_url`     | capture-ledger API の宛先（既定 `http://192.168.64.1:7070`） | 同上                            |
| `u/admin/browserhive_target` | gRPC の宛先 `browserhive.capture-ledger:50051`               | 同上                            |
| `u/admin/browserhive_tls_ca` | TLS の CA（空 = 平文）                                       | 同上                            |
| `u/admin/browserhive_proto`  | `capture.proto` の写し（`crawl_host` が読む）                | `windmill:push-proto`           |

**`windmill:push-proto` は `windmill:push` に含まれない。** proto を取り直したら
これも実行し直すこと —— Windmill が読むのはこの写しで、submodule ではない。

## 全部消えたときの復旧

windmill-db の volume を消すと、workspace・token・変数が全部消える。
復旧の順序（2026-09-11 に実測）:

```sh
pnpm run windmill:bootstrap            # workspace を作り直し、新しい token を出す
                                       # → 出た WINDMILL_TOKEN= を .env に貼り直す
pnpm run windmill:push                 # script / flow / schedule
pnpm run windmill:push-proto           # ← 忘れると crawl_host が Resource not found
pnpm run windmill:capture-ledger-token # 変数 4 つ
cd ../capture-ledger
pnpm run fga:grant submitter windmill acme
```

capture-ledger 側の OpenFGA も同時に消えていたら、grant の前に
`fga:migrate` → `fga:deploy` で store を作り直し、出てきた 2 つの ID を
あちらの `.env` に貼る。

**コンテナを作り直すと bridge の subnet が変わりうる。**
`u/admin/waggle_api_url` の既定は `192.168.64.1` で、subnet が変わると
report の段が `ConnectionRefused` になる。いまの gateway は `ifconfig` の
`bridge100` で分かる。直すのは `.env` の `CAPTURE_LEDGER_API_URL` を書いて
`windmill:capture-ledger-token` を再実行。届いているかは実測できる:

```sh
container exec windmill.capture-scheduler \
  curl -s -o /dev/null -w '%{http_code}\n' -X POST http://192.168.66.1:7070/api/crawls
# 401 なら届いている（認証待ち）。000 なら届いていない
```

## `f/waggle/` という名前

repo は waggle → capture-ledger と改名したが、Windmill の folder は
`f/waggle/` のまま。**folder のパスは webhook URL の一部**
（`/api/w/crawler/jobs/run/f/f/waggle/crawl_level`）で、変えると
capture-ledger 側に設定した webhook が壊れる。名前ではなく外部契約として
据え置いてある。
