# capture-scheduler の設定。**このファイルが .env の唯一の出どころ** —— setup.sh はこれを写すだけ。
#
# 書いてよい行は 2 つの形だけ:
#   NAME=value    値がある
#   #NAME=value   既定値がある。変えたいときだけコメントを外す
#
# 生の `NAME=` (空文字) は書かないこと。空文字は「無い」より悪い ——
# 既定値を潰したうえで、名前が一言も出ないエラーになる。

# ── Windmill ────────────────────────────────────────────────────────────

# Windmill の URL。host 側の道具 (この repo の scripts/) が使う。
#WINDMILL_URL=http://127.0.0.1:8000

# workspace の id。scripts/bootstrap.mjs が無ければ作る。
#WINDMILL_WORKSPACE=crawler

# bootstrap が作った API token。**`pnpm run windmill:bootstrap` の出力を貼る。**
# 起動前には決まらないので、setup.sh は空のまま残す。
WINDMILL_TOKEN=

# Windmill の初回ログイン。上流の既定のまま。
#WINDMILL_EMAIL=admin@windmill.dev
#WINDMILL_PASSWORD=changeme

# ── capture-ledger ──────────────────────────────────────────────────────────────

# **コンテナから見た** capture-api の URL。host は既定のブリッジ gateway。
# capture-ledger 側で CAPTURE_LEDGER_API_HOST=0.0.0.0 にしていないと、ここには届かない。
#CAPTURE_LEDGER_API_URL=http://192.168.64.1:7070

# host から見た capture-ledger の dev OIDC issuer。**loopback のままにすること** ——
# この issuer は頼まれれば誰の名前でもトークンを出すので、外に出した瞬間に
# 「ブリッジに届く誰もが windmill を名乗れる」になる。
# 鍵を作るのは host、コンテナへ渡すのは出来上がったトークンだけ。
#CAPTURE_LEDGER_OIDC_ISSUER=http://127.0.0.1:9099

# トークンに載せる主体と組織。この主体に submitter の付与が要る:
#   cd ../capture-ledger && pnpm run fga:grant submitter windmill acme
#CAPTURE_LEDGER_SUBJECT=windmill
#CAPTURE_LEDGER_ORGANIZATIONS=acme

# トークンの寿命 (jose の綴り)。issuer を再起動すると鍵ごと変わるので、
# どれだけ長くしても再起動には勝てない。
#CAPTURE_LEDGER_TOKEN_EXPIRES_IN=30d

# コンテナから見た browserhive の gRPC の口。カンマ区切りで複数 —— browser 1 台に
# 口 1 つなので、並列に取り込む数だけ並ぶ。Windmill の script が空いている口を選ぶ。
# **変数に入れて渡す** —— Windmill は schema の既定値を UI からの実行にしか埋めないので、
# webhook で起こすと引数は素通りになる。
#CAPTURE_LEDGER_BROWSERHIVE_ENDPOINTS=browserhive-1.capture-ledger:50051,browserhive-2.capture-ledger:50051
# browserhive への gRPC を TLS にするときの CA 証明書 (PEM の中身)。空 = 平文。
#CAPTURE_LEDGER_BROWSERHIVE_TLS_CA_PEM=
